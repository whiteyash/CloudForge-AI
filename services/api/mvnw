#!/usr/bin/env sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
MAVEN_BIN="$SCRIPT_DIR/mvn_dist/apache-maven-3.9.9/bin/mvn"

if [ ! -x "$MAVEN_BIN" ]; then
  echo "Unable to find Maven at $MAVEN_BIN" >&2
  exit 1
fi

exec "$MAVEN_BIN" "$@"
